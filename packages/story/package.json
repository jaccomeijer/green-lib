{
  "name": "@local/story",
  "version": "1.0.0",
  "scripts": {
    "build": "No build needed, this package is never published",
    "clean": "rm -rf dist",
    "serve": "npm run clean && MODE=serve node story.js"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "type": "module",
  "devDependencies": {
    "@jaccomeijer/green-lib": "1.2.15"
  }
}
