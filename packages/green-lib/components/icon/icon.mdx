---
navigation:
  heading: Icon
  parent: Components
---
export { Layout as default } from '@local/story/layout/layout.jsx'
import { Icon } from './icon.jsx'
import { globals } from '@local/story/config/globals.js'

## {frontmatter.navigation.heading}
## Feather icons
See: [https://feathericons.com](https://feathericons.com)

### Example
```html
<p class="u-font-size-0 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>
```
<p class="u-font-size-0 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>

### Example
```html
<p class="u-font-size-1 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>
```
<p class="u-font-size-1 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>

### Example
```html
<p class="u-font-size-2 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>
```
<p class="u-font-size-2 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>

### Example
```html
<p class="u-font-size-3 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>
```
<p class="u-font-size-3 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>

### Example
```html
<p class="u-font-size-4 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>
```
<p class="u-font-size-4 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>

### Example
```html
<p class="u-font-size-5 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>
```
<p class="u-font-size-5 with-icon u-color-primary-400"><Icon globals={globals} icon="camera" />Camera</p>
