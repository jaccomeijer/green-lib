---
navigation:
  heading: button
  parent: Components
---
export { Layout as default } from '@local/story/layout/layout.jsx'
import { Icon } from './icon/icon.jsx'
import { globals } from '@local/story/config/globals.js'

## {frontmatter.navigation.parent} - {frontmatter.navigation.heading}
### Example
```html
<button className="button u-border-radius-rounded">Take photo</button>
```
<button className="button u-border-radius-rounded">Take photo</button>

### Example
```html
<button className="button u-border-radius-rounded u-font-size-1">Take photo</button>
```
<button className="button u-border-radius-rounded u-font-size-1">Take photo</button>

### Example
```html
<button className="button u-border-radius-rounded u-font-size-2">Take photo</button>
```
<button className="button u-border-radius-rounded u-font-size-2">Take photo</button>

### Example
```html
<button className="button u-border-radius-rounded u-font-size-3">Take photo</button>
```
<button className="button u-border-radius-rounded u-font-size-3">Take photo</button>

### Example
```html
<button className="button u-border-radius-rounded u-font-size-0 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>
```
<button className="button u-border-radius-rounded u-font-size-0 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>

### Example
```html
<button className="button u-border-radius-rounded u-font-size-1 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>
```
<button className="button u-border-radius-rounded u-font-size-1 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>

### Example
```html
<button className="button u-border-radius-rounded u-font-size-2 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>
```
<button className="button u-border-radius-rounded u-font-size-2 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>

### Example
```html
<button className="button u-border-radius-rounded u-font-size-3 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>
```
<button className="button u-border-radius-rounded u-font-size-3 with-icon"><Icon globals={globals} icon="camera" />Take photo</button>
