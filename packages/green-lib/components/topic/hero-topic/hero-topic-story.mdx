---
navigation:
  heading: HeroTopic
  parent: Topic
topic:
  heading: Hero example topic
  labels: [opinion, test, demo, story]
  abstract: This is an example of a topic abstract text.
  image: topicImage
  icon: share
  action:
    heading: Test action
    icon: archive
---
export { StoryLayout as default } from '@local/story/components/story-layout/story-layout.jsx'
import { HeroTopic } from './hero-topic'
import topicImage from '@local/story/assets/images/rianne-zuur-PoxFTU7_QKo-unsplash.jpg'
import { globals } from '@local/story/config/globals.js'

# {frontmatter.navigation.heading}
## Frontmatter
```yaml
topic:
  heading: Hero example topic with a long heading
  labels: [opinion, test, demo, story]
  abstract: This is an example of a topic abstract text.
  image: topicImage
  icon: share
  action:
    heading: Test action
    icon: archive
```

## Example
```html
<HeroTopic
  topic={{...frontmatter.topic, image: undefined}}
  globals={globals}
/>
```
<HeroTopic
  topic={{...frontmatter.topic, image: undefined}}
  globals={globals}
/>

## Example
```js
import topicImage from '../../../story/assets/images/pexels-skyler-ewing-4596712.jpg'
```
```html
<div class="container-full">
  <HeroTopic
    topic={frontmatter.topic}
    images={{ topicImage }}
    globals={globals}
  />
</div>
```
<div class="container-full">
  <HeroTopic
    topic={frontmatter.topic}
    images={{ topicImage }}
    globals={globals}
  />
</div>
