---
navigation:
  heading: Topic Hero
  parent: Topic
topic:
  heading: Hero example topic
  labels: [opinion, test, demo, story]
  abstract: This is an example of a balanced topic abstract text that is placed below the header.
  image: topicImage
  icon: share
  action:
    heading: Test action
    icon: archive
---
export { Layout as default } from '@local/story/layout/layout.jsx'
import { Topic } from './topic.jsx'
import topicImage from '@local/story/assets/images/rianne-zuur-PoxFTU7_QKo-unsplash.jpg'
import { globals } from '@local/story/config/globals.js'

## {frontmatter.navigation.heading}
### Example
```html
<Topic
  topic={frontmatter.topic}
  images={{ topicImage }}
  globals={globals}
  data-variant="hero"
/>
```
<Topic
  topic={frontmatter.topic}
  images={{ topicImage }}
  globals={globals}
  data-variant="hero"
/>

### Example
```html
<Topic
  topic={{...frontmatter.topic, image: undefined}}
  globals={globals}
  data-variant="hero"
/>
```
<Topic
  topic={{...frontmatter.topic, image: undefined}}
  globals={globals}
  data-variant="hero"
/>

### Example
```html
<div class="s-popout">
  <Topic
    topic={frontmatter.topic}
    images={{ topicImage }}
    globals={globals}
    data-variant="hero"
  />
</div>
```

<div class="s-popout">
  <Topic
    topic={frontmatter.topic}
    images={{ topicImage }}
    globals={globals}
    data-variant="hero"
  />
</div>

### Frontmatter
```yaml
topic:
  heading: Hero example topic
  labels: [opinion, test, demo, story]
  abstract: This is an example of a balanced topic abstract text that is placed below the header.
  image: topicImage
  icon: share
  action:
    heading: Test action
    icon: archive
```
