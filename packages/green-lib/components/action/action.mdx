---
navigation:
  heading: Action
  parent: Components

action:
  heading: action
  icon: circle
  url: /demo-url-with-icon

actionWithHref:
  heading: actionWithHref
  icon: circle
  url: https://www.modelberry.com

actionWithoutIcon:
  heading: actionWithoutIcon
  url: /demo-url
---
export { Layout as default } from '@local/story/layout/layout.jsx'
import { Action } from './action'
import { globals } from '@local/story/config/globals.js'

## {frontmatter.navigation.heading}
### Example
```html
<Action
  action={frontmatter.action}
  className="u-font-size-0"
  globals={globals}
  element="button"
/>
```
<Action
  action={frontmatter.action}
  className="u-font-size-0"
  globals={globals}
  element="button"
/>

### Example
```html
<Action
  action={frontmatter.action}
  className="u-font-size-0"
  globals={globals}
  element="a"
/>
```
<Action
  action={frontmatter.action}
  className="u-font-size-0"
  globals={globals}
  element="a"
/>

### Example
```html
<Action
  action={frontmatter.action}
  className="fat-link u-font-size-0"
  globals={globals}
  element="a"
/>
```
<Action
  action={frontmatter.action}
  className="fat-link u-font-size-0"
  globals={globals}
  element="a"
/>

### Example
```html
<Action
  action={frontmatter.actionWithHref}
  className="u-font-size-2"
  globals={globals}
  element="button"
/>
```
<Action
  action={frontmatter.actionWithHref}
  className="u-font-size-2"
  globals={globals}
  element="button"
/>

### Example
```html
<Action
  action={frontmatter.actionWithHref}
  className="u-font-size-2"
  globals={globals}
  element="a"
/>
```
<Action
  action={frontmatter.actionWithHref}
  className="u-font-size-2"
  globals={globals}
  element="a"
/>

### Example
```html
<Action
  action={frontmatter.actionWithoutIcon}
  className="u-font-size-4"
  globals={globals}
  element="button"
/>
```
<Action
  action={frontmatter.actionWithoutIcon}
  className="u-font-size-4"
  globals={globals}
  element="button"
/>

### Example
```html
<Action
  action={frontmatter.actionWithoutIcon}
  className="u-font-size-4"
  globals={globals}
  element="a"
/>
```
<Action
  action={frontmatter.actionWithoutIcon}
  className="u-font-size-4"
  globals={globals}
  element="a"
/>

### Frontmatter
```yaml
action:
  icon: circle
  heading: action
  url: /demo-url-with-icon

actionWithHref:
  heading: actionWithHref
  url: https://www.modelberry.com

actionWithoutIcon:
  heading: actionWithoutIcon
  url: /demo-url
```
