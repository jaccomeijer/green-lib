---
navigation:
  heading: Segment
  parent: Navigation
  url: /override/default/url

menu:
  - heading: Item A
    icon: archive
    url: /demo-a
  - heading: Item B
    url: /demo-b
  - heading: Item C
    icon: clock
    url: /demo-c
---
export { StoryLayout as default } from '@local/story/components/story-layout.jsx'
import { Segment } from './segment.jsx'
import ceCss from '../navigation.ce.css'
import { globals } from '@local/story/config/globals.js'

# {frontmatter.navigation.heading}
## Frontmatter
```yaml
navigation:
  parent: Navigation
  url: /override/default/url

menu:
  - heading: Item A
    icon: archive
    url: /demo-a
  - heading: Item B
    url: /demo-b
  - heading: Item C
    icon: clock
    url: /demo-c
```


## Example
```js
export const assetUrl = globals.baseUrl + globals.assetUrl
```
```html
<link rel="stylesheet" type="text/css" href={assetUrl + ceCss } />
<Segment globals={globals} pageUrl="/demo-b" menu={frontmatter.menu} />
```

export const assetUrl = globals.baseUrl + globals.assetUrl

<link rel="stylesheet" type="text/css" href={assetUrl + ceCss } />
<Segment globals={globals} pageUrl="/demo-b" menu={frontmatter.menu} />
