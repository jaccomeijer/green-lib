---
navigation:
  heading: IconButton
  parent: Element

action:
  heading: Demo action
  url: /demo-url

actionWithIcon:
  icon: circle
  heading: Demo actionWithIcon
  url: /demo-url-with-icon

actionWithHref:
  heading: Demo actionWithHref
  url: https://www.modelberry.com
---
export { StoryLayout as default } from '@local/story/components/story-layout/story-layout.jsx'
import { IconButton } from './icon-button'
import { globals } from '@local/story/config/globals.js'

# {frontmatter.navigation.heading}
## frontmatter
```yaml
action:
  heading: Demo action
  url: /demo-url

actionWithIcon:
  icon: circle
  heading: Demo actionWithIcon
  url: /demo-url-with-icon

actionWithHref:
  heading: Demo actionWithHref
  url: https://www.modelberry.com
```

## Example
```html
<IconButton globals={globals} variant="s" action={frontmatter.action}>child</IconButton>
```
<IconButton globals={globals} variant="s" action={frontmatter.action}>child</IconButton>

## Example
```html
<IconButton globals={globals} variant="m" action={frontmatter.action} />
```
<IconButton globals={globals} variant="m" action={frontmatter.action} />

## Example
```html
<IconButton globals={globals} variant="l" action={frontmatter.action} />
```
<IconButton globals={globals} variant="l" action={frontmatter.action} />

## Example
```html
<IconButton globals={globals} variant="s" action={frontmatter.actionWithIcon} />
```
<IconButton globals={globals} variant="s" action={frontmatter.actionWithIcon} />

## Example
```html
<IconButton globals={globals} variant="m" action={frontmatter.actionWithIcon} />
```
<IconButton globals={globals} variant="m" action={frontmatter.actionWithIcon} />

## Example
```html
<IconButton globals={globals} variant="l" action={frontmatter.actionWithIcon} />
```
<IconButton globals={globals} variant="l" action={frontmatter.actionWithIcon} />

## Example
```html
<IconButton globals={globals} variant="m" action={frontmatter.actionWithHref} />
```
<IconButton globals={globals} variant="m" action={frontmatter.actionWithHref} />
