---
navigation:
  heading: IconButton
  parent: Element
---
export { StoryLayout as default } from '../../../../stories/story-layout.jsx'
import { IconButton } from './icon-button'
import { globals } from '../../../../data/globals.js'

# {frontmatter.navigation.heading}
## Example
```html
<IconButton globals={globals} url="/demo-url">Small button</IconButton>
```
<IconButton globals={globals} url="/demo-url">Small button</IconButton>

## Example
```html
<IconButton globals={globals} url="/demo-url" variant="s">Small button</IconButton>
```
<IconButton globals={globals} url="/demo-url" variant="s">Small button</IconButton>

## Example
```html
<IconButton globals={globals} url="/demo-url" variant="m">Medium button</IconButton>
```
<IconButton globals={globals} url="/demo-url" variant="m">Medium button</IconButton>

## Example
```html
<IconButton globals={globals} url="/demo-url" variant="l">Large button</IconButton>
```
<IconButton globals={globals} url="/demo-url" variant="l">Large button</IconButton>

## Example
```html
<IconButton globals={globals} icon="circle" variant="s" />
```
<IconButton globals={globals} icon="circle" variant="s" />

## Example
```html
<IconButton globals={globals} icon="circle" variant="m" />
```
<IconButton globals={globals} icon="circle" variant="m" />

## Example
```html
<IconButton globals={globals} icon="circle" variant="s">Small button</IconButton>
```
<IconButton globals={globals} icon="circle" variant="s">Small button</IconButton>

## Example
```html
<IconButton globals={globals} icon="share" variant="m">Medium button</IconButton>
```
<IconButton globals={globals} icon="share" variant="m">Medium button</IconButton>

## Example
```html
<IconButton globals={globals} icon="archive" variant="l">Large button</IconButton>
```
<IconButton globals={globals} icon="archive" variant="l">Large button</IconButton>

## Example
```html
<IconButton globals={globals} icon="github" variant="s" class="stretch">Small button</IconButton>
```
<IconButton globals={globals} icon="github" variant="s" class="stretch">Small button</IconButton>

## Example
```html
<IconButton globals={globals} icon="github" variant="l" class="outline">Large button</IconButton>
```
<IconButton globals={globals} icon="github" variant="l" class="outline">Large button</IconButton>
