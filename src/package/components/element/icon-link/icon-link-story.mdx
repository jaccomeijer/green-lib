---
navigation:
  heading: IconLink
  parent: Element
---
export { StoryLayout as default } from '../../../../stories/story-layout.jsx'
import { IconLink } from './icon-link'
import { globals } from '../../../../data/globals.js'

# {frontmatter.navigation.heading}
## Example
```html
<p variant="s">Text with a <IconLink globals={globals} url="/demo-url">small link</IconLink> inside.</p>
```
<p variant="s">Text with a <IconLink globals={globals} url="/demo-url">small link</IconLink> inside.</p>

## Example
```html
<p variant="m">Text with a <IconLink globals={globals} url="/demo-url">medium link</IconLink> inside.</p>
```
<p variant="m">Text with a <IconLink globals={globals} url="/demo-url">medium link</IconLink> inside.</p>

## Example
```html
<p variant="l">Text with a <IconLink globals={globals} url="/demo-url">large link</IconLink> inside.</p>
```
<p variant="l">Text with a <IconLink globals={globals} url="/demo-url">large link</IconLink> inside.</p>

## Example
```html
<IconLink globals={globals} icon="circle" variant="s">small icon link</IconLink>
```
<IconLink globals={globals} icon="circle" variant="s">small icon link</IconLink>

## Example
```html
<IconLink globals={globals} icon="github" variant="m">medium icon link</IconLink>
```
<IconLink globals={globals} icon="github" variant="m">medium icon link</IconLink>

## Example
```html
<IconLink globals={globals} icon="github" variant="l">large icon link</IconLink>
```
<IconLink globals={globals} icon="github" variant="l">large icon link</IconLink>
