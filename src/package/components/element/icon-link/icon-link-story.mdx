---
navigation:
  heading: IconLink
  parent: Element

action:
  heading: Demo action
  url: /demo-url

actionWithIcon:
  icon: circle
  heading: Demo actionWithIcon
  url: /demo-url-with-icon

actionWithHref:
  heading: Demo actionWithHref
  url: https://www.modelberry.com
---
export { StoryLayout as default } from '../../../../stories/story-layout.jsx'
import { IconLink } from './icon-link'
import { globals } from '../../../../data/globals.js'

# {frontmatter.navigation.heading}
## frontmatter
```yaml
action:
  heading: Demo action
  url: /demo-url

actionWithIcon:
  icon: circle
  heading: Demo actionWithIcon
  url: /demo-url-with-icon

actionWithHref:
  heading: Demo actionWithHref
  url: https://www.modelberry.com
```

## Example
```html
<p variant="m">Text with a <IconLink globals={globals} action={frontmatter.action}>child</IconLink> inside.</p>
```
<p variant="m">Text with a <IconLink globals={globals} action={frontmatter.action}>child</IconLink> inside.</p>

## Example
```html
<p variant="s">Text with a <IconLink globals={globals} action={frontmatter.action} /> inside.</p>
```
<p variant="s">Text with a <IconLink globals={globals} action={frontmatter.action} /> inside.</p>

## Example
```html
<p variant="m">Text with a <IconLink globals={globals} action={frontmatter.action} /> inside.</p>
```
<p variant="m">Text with a <IconLink globals={globals} action={frontmatter.action} /> inside.</p>

## Example
```html
<p variant="l">Text with a <IconLink globals={globals} action={frontmatter.actionWithIcon} /> inside.</p>
```
<p variant="l">Text with a <IconLink globals={globals} action={frontmatter.actionWithIcon} /> inside.</p>

## Example
```html
<IconLink globals={globals} variant="s" action={frontmatter.actionWithIcon} />
```
<IconLink globals={globals} variant="s" action={frontmatter.actionWithIcon} />

## Example
```html
<IconLink globals={globals} variant="m" action={frontmatter.actionWithIcon} />
```
<IconLink globals={globals} variant="m" action={frontmatter.actionWithIcon} />

## Example
```html
<IconLink globals={globals} variant="l" action={frontmatter.actionWithIcon} />
```
<IconLink globals={globals} variant="l" action={frontmatter.actionWithIcon} />

## Example
```html
<IconLink globals={globals} variant="m" action={frontmatter.actionWithHref} />
```
<IconLink globals={globals} variant="m" action={frontmatter.actionWithHref} />
