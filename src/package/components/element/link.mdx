---
navigation:
  heading: link
  parent: Element
---
export { StoryLayout as default } from '../../../stories/story-layout.jsx'

# {frontmatter.navigation.heading}
## Example
```html
<p>A paragraph with a <a href="#">link</a> inside.</p>
```
<p>A paragraph with a <a href="#">link</a> inside.</p>

## Example
```html
<p variant="xl">Extra large text with an active <a href="#" class="active">link</a> inside.</p>
```
<p variant="xl">Extra large text with an active <a href="#" class="active">link</a> inside.</p>
